# Локальный RAG (Retrieval-Augmented Generation) система

Система для семантического поиска и генерации ответов на основе локальных документов с использованием технологии RAG.

## 🚀 Основные возможности

- **Локальное выполнение** - все вычисления на вашем компьютере
- **Поддержка форматов** - PDF и DOCX документы  
- **Многоязычность** - работа с русскими и английскими текстами
- **Контекстная память** - запоминание истории диалога
- **Векторный поиск** - семантический поиск по документам

## 📦 Используемые технологии

### 🧠 Языковые модели
- **Ollama** - инструмент для запуска локальных LLM
- **qwen2.5-coder:7b** - основная модель для генерации ответов
- **intfloat/multilingual-e5-large** - модель для векторных эмбеддингов

### 🔧 Основные библиотеки
- **LangChain** - фреймворк для работы с LLM
- **FAISS** - векторное хранилище для семантического поиска
- **Streamlit** - веб-интерфейс для взаимодействия

## ⚡ Быстрый старт

### 1. Установка Ollama
```bash
# Скачайте и установите с https://ollama.com/
# Для Windows: https://ollama.com/download/windows
```

### 2. Установка модели
```bash
ollama pull qwen2.5-coder:7b
```

### 3. Настройка окружения
```bash
# Клонируйте репозиторий
git clone <your-repo>
cd ai-assistant

# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

# Установите зависимости
pip install -r requirements.txt
```

### 4. Подготовка документов
```bash
# Поместите PDF/DOCX файлы в папку pdf/
mkdir pdf
# Добавьте ваши документы в эту папку
```

### 5. Запуск системы

#### Вариант A: Командная строка
```bash
cd Python
python Simple_RAG_PDF.py
```

#### Вариант B: Веб-интерфейс
```bash
cd Python
streamlit run st.py
```
Откройте http://localhost:8501 в браузере

## 📁 Структура проекта

```
ai-assistant/
├── Python/                 # Основной код
│   ├── pdf/               # Документы для обработки
│   ├── db/                # Векторная база данных
│   ├── log/               # Логи выполнения
│   ├── Simple_RAG_PDF.py  # Основной RAG модуль
│   └── st.py              # Streamlit интерфейс
├── requirements.txt        # Зависимости Python
└── README.md              # Документация
```

## 🔧 Настройка

### Модели по умолчанию
- **LLM**: `qwen2.5-coder:7b` - для генерации ответов
- **Эмбеддинги**: `intfloat/multilingual-e5-large` - для векторного поиска

### Параметры обработки
- Размер чанков: 1024 символа
- Количество релевантных фрагментов: 3
- Размер окна памяти: 6 сообщений

## 💡 Пример использования

1. Запустите систему через Streamlit
2. Загрузите документы в папку `pdf/`
3. Задавайте вопросы на естественном языке
4. Система найдет релевантные фрагменты и сгенерирует ответ

**Пример вопросов:**
- "Какие основные темы обсуждаются в документах?"
- "Найди информацию о конкретном термине"
- "Объясни концепцию из документа"

## 🛠️ Разработка

### Добавление новых форматов документов
Реализуйте новые загрузчики в функции `load_documents_from_directory()`

### Настройка промптов
Измените шаблоны в функции `create_prompt_template()`

### Оптимизация производительности
- Настройте размер чанков в `CHUNK_SIZE`
- Измените количество релевантных фрагментов в `DEFAULT_RELEVANT_CHUNKS`

---